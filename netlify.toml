[build]
  base    = "procurement-app/frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — all paths served from index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
